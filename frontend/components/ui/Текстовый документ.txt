"use client"; // клиентский компонент

import { AnimatePresence, motion } from 'framer-motion'

export default function CatalogExample() {

    return (
        <div className="flex flex-col gap-2">
            <AnimatePresence>
                {[1, 2, 3, 4, 5].map((num) => (
                    <motion.div initial={{scale:0.0, opacity:0.0, translateY: 10}} animate={{scale:1.0, opacity:1.0, translateY: 0}} className="rounded-2xl flex flex-col items-center jusitfy-center shadow-lg p-4">
                        <div>Блок {num}</div>
                    </motion.div>
                ))}
            </AnimatePresence>
        </div>
    )
}